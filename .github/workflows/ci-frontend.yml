name: CI for Frontend

on:
  push:
    branches: [ "main" ]
    paths:
      - 'draw.it.client/**'
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'draw.it.client/**'
    paths-ignore:
      - '**.md'

jobs:
  lint:
    name: 🧹 Lint Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint
        uses: ./.github/actions/node-lint
